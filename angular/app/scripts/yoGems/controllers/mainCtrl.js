'use strict';
/**
 * @ngdoc function
 * @name sbAdminApp.controller:MainCtrl
 * @description
 * # MainCtrl
 * Controller of the sbAdminApp
 */
angular.module('sbAdminApp')
  .controller('mainCtrl',['$rootScope',
        function($rootScope) {
            
            $rootScope.mainClassForBody = 'ddd';

  }]);
